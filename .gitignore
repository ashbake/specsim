# python build/cache artifacts
specsim.egg-info/
__pycache__/
*.pyc
*~
.pytest_cache/
.venv/
.ipynb_checkpoints/

.DS_Store

# plots written to the repo root by the examples / plot_tools default savepath
/*.png

# large data files that are downloaded rather than committed (see README);
# everything else under data/ ships with the repo
data/telluric/psg_out_2020.08.02_l0_800nm_l1_2700nm_res_0.001nm_lon_204.53_lat_19.82_pres_0.5826.fits

# the per-subsystem throughput curves the base-throughput files were built
# from. Superseded by transmission_file; kept locally for the budget plots in
# specsim.plot, which now take a subsystems_path rather than defaulting here
data/instrument/*/throughput/old/
